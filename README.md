🧮 Python Calculator

A simple and beginner-friendly command-line calculator written in Python.
It performs basic arithmetic operations like:-

->Addition
->Subtraction
->Multiplication
->Division

This project was created locally and pushed to a GitHub repository using the VS Code terminal.
📌 Features:-

->Beginner-friendly code
->Accepts user input
->Menu-based calculator
->Shows results for all arithmetic operations
->Handles invalid options

🚀 How the Project Was Pushed Using the VS Code Terminal
Below are the exact steps used in VS Code’s integrated terminal to push the project to GitHub:

**1. Open VS Code Terminal**
Go to:
View → Terminal

or press:
Ctrl + `

**2. Initialize Git**
git init

**3. Add all files**
git add .

**4. Commit the project**
git commit -m "Initial commit: Added Python calculator"

**5. Add the GitHub remote**
git remote add origin https://github.com/<your-username>/<your-repo-name>.git

6. **Push the code to GitHub**
If your default branch is main:

git push -u origin main

If your repo used master, then:
git push -u origin master
